/* ADVENTURE TIME API SOLUTION WALKTHROUGH

1. Create HTML and JS files
2. Set up HTML Boilderplate (!  + tab)
3. Connect the JS file with a <script> tag and use a console.log to ensure they are connected
4. Set up the url.
5. In the HTML file, add a <div> tag in the <body> tag.
6. Query the <div> tag. Either with a querySelector('div'), querySelector('.quotes') or getElementById('quotes);
7. Set up the image links as an object
8. Set up the syntax for the fetch

  fetch(<url always goes here>)
    .then()
    .then()
    .catch()
*/